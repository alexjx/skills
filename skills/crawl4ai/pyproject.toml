[project]
name = "crawl4ai-skill"
version = "0.1.0"
description = "Claude skill for web crawling with crawl4ai"
requires-python = ">=3.10"
dependencies = [
    "crawl4ai",
]

[project.scripts]
crawl = "scripts.crawl:main"
